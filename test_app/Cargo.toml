[package]
name = "test_app"
version = "0.1.0"
edition = "2024"

[dependencies]
inbox = { path = "../inbox", features = ["amqp","diesel"] }
tokio = { version = "1", features = ["macros", "rt-multi-thread", "signal"] }
tokio-util = { version = "0.7" }
anyhow = "1"
