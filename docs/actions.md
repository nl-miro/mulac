


### Ask for review

```
As a staff engineer, review XXX implementation now and store it in folder docs/code-reviews as {timestamp}-review-{toolname}-{model}-{effort}.md

Document should have each issue in details, then each issue summarized and at the end actionable checklist ordered in a way that blockers come before blocked code.

```



```
As a system architect, take a look at inbox library and create review of the architecture it self, store it in folder docs/code-reviews as {timestamp}-review-{toolname}-{model}-{effort}.md

Each item should have details, offering multiple solutions.

```







