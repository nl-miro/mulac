# Rust code style

## Imports in code and tests

- No empty lines between import statements
- Multi line imports should use `//` after the last import and before closing brace. Brace should always be in new line so that formatter keeps them at 1 binding per line
- Multi line imports should be max 80 chars per line
 